# ─── Loan Charges Calculator Engine — dependencies ───
# fmm_calculator.py (the engine) and fmm_server.py (the API server that wraps
# it) are both standard-library-only, by design — see fmm_calculator.py's
# module docstring. There is NOTHING to pip install to run either one.
#
# Deploying fmm_server.py: the start command is just
#     python fmm_server.py --host 0.0.0.0
# It is a plain http.server app, not FastAPI/Flask/Django — no ASGI/WSGI
# entry point, no "app" object. Do not point a Uvicorn/Gunicorn start
# command at it.
#
# If you later want to model the quote structure as typed objects instead of
# raw dicts, or need to read/write legacy .xlsx calculators, add:
#   pydantic>=2.9
#   openpyxl>=3.1
